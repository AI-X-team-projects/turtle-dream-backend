server:
  port: 8080

spring:
  data:
    autoconfigure:
      exclude: org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
    mongodb:
      host: localhost
      port: 27017
      database: turtledream

  websocket:
    max-text-message-size: 10MB
    max-binary-message-size: 10MB
